class Account: # NOTE: User must be using gmail account.
	def __init__(self):
		self.user = 'your gmail'
		self.password = 'your gmail password'
                self.receiver = 'your receivers' #NOTE: If more than one receiver, make it a list of strings with their email addresses.
